apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tenant-c-stg

resources:
  - ../../../base

namePrefix: app-c1-

commonLabels:
  tenant: tenant-c
  app: app-c1
  environment: stg

patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
    target:
      kind: Deployment
      name: app
  - patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: AppC1-stg.agent.opsera.io
      - op: replace
        path: /spec/tls/0/hosts/0
        value: AppC1-stg.agent.opsera.io
    target:
      kind: Ingress
      name: app

configMapGenerator:
  - name: app-config
    literals:
      - ENVIRONMENT=stg
      - TENANT=tenant-c
      - APP_NAME=app-c1





